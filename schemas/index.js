const { addSchema, updateFavoriteSchema } = require("./contacts");
const { authSchema, updateSubscriptionSchema } = require("./users");

module.exports = {
  addSchema,
  updateFavoriteSchema,
  authSchema,
  updateSubscriptionSchema,
};
